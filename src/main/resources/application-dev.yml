spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:tcp://localhost/~/my-blog;MODE=MYSQL;DB_CLOSE_DELAY=-1"
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create

  security:
    user:
      name: user
      password: 1234